{
    "tasks": [
        {
            "type": "shell",
            "label": "Make",
            "command": "make -j",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        },
        {
            "type": "shell",
            "label": "Make debug",
            "command": "make -j debug",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        },
        {
            "type": "shell",
            "label": "Make debug (fatal)",
            "command": "make debug",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        },
        {
            "type": "shell",
            "label": "Make release",
            "command": "make -j release",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        },
        {
            "type": "shell",
            "label": "Make test",
            "command": "make test",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ],
    "version": "2.0.0"
}